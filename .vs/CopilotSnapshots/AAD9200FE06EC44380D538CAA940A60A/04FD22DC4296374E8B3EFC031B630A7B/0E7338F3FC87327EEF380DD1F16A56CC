using MediatR;

namespace CleanArchitecture.Full.Application.Accounts.Commands.CreateAccount;

public record CreateAccountCommand(
    Guid ClientId,
    string AccountNumber,
    string HolderName,
    decimal Balance,
    string Status,
    string AccountType,
    DateTime OpenedAt) : IRequest<AccountDto>;
